{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "set NODE_ENV=production && ts-node -r tsconfig-paths/register src/main.ts",
  "env": {
    "production": {
      "exec": "set NODE_ENV=production && node -r tsconfig-paths/register dist/main.js"
    }
  }
}
